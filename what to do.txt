node index.js

/// after installing nodemon & adding script to package.json

npm run dev

// but after combined servers, express server runs with:

npm run server

// combined (with concurrently) server runs with 

npm run dev

todo:

